# Spec-45 (#405) T10 (#498): the app default UI language is Ukrainian
# (AppLocale default), so the default Robolectric resources configuration
# must be uk — otherwise every UI test would resolve values-en and fail
# on the Ukrainian assertions. Tests exercising another locale opt in
# with @Config(qualifiers = ...) instead.
qualifiers=uk
