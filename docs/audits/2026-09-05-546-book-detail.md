# Сторінка книги — #546

Зміни підготовлено в `codex/release-1.3.9-preparation` від `9d817b73`.

## Що виправлено

- Зірки закладок стоять безпосередньо біля автора й озвучувача. Довгі імена переносяться; для кнопки залишається зона натискання 48 dp.
- Однакові пари джерела й адреси показуються один раз. Інші адреси того самого джерела зберігаються.
- Прибрано кнопку «Всесвіт неправильний?». Назва всесвіту залишається.
- Прибрано постійний лейбл «Потрібен браузер» і його текст у стані доступності. Логіку відновлення та перевірки сайту не змінено.

## Перевірка

Новий тест дублювання джерел спочатку впав, після виправлення пройшов.

- `BookDetailPresentationTest` через `testPureJvm` — пройшов.
- `BookDetailAccessibilityTest` через `testComposeRoborazzi` — пройшов.
- `assembleDebug`, `assembleDebugAndroidTest` — пройшли.
- `BookDetailPeopleLayoutTest` на погодженому телефоні — 1 тест, 2,934 с, успішно. Перевірено перенос довгих імен на ширині 320 dp, розташування й натискання обох зірок, відсутність зайвих текстів і збереження іншої адреси 4read. Тест використовує окремий стан інтерфейсу, не змінює бібліотеку чи налаштування телефона.

Актуальний debug APK установлено зі збереженням даних. SHA-256: `97d39642e59647d5b39d6e18b79c9451a01230652a8bba253fc38fedd1d87cd9`.

Вручну переглянуто знімок тестового екрана та справжню сторінку «Проблема з миром»: зірки біля імен, всесвіт збережено, одне джерело 4read, лейбла браузера немає.

Локальні знімки не публікуються разом із репозиторієм:

- `/tmp/slukhayka-546-people-layout.png`
- `/tmp/slukhayka-546-detail.png`
- `/tmp/slukhayka-546-sources.png`

## Рев’ю та межі

Окремі рев’ю відповідності задачі та стандартам не знайшли блокувальних дефектів. Зауваження до залежності тесту від мови враховано: описи кнопок беруться з ресурсів.

Повний `scripts/test-all.sh` запущено на вимогу implement з тимчасовим обмеженням дві хвилини на тестову задачу Gradle. Pure JVM і Room native SDK 35 пройшли. Room native SDK 36 перевищив час очікування; скрипт завершився з кодом 1, наступні групи не запускалися. Це не повний зелений прогін і не встановлена причина зависання. Повну перевірку треба повторити в CI. Лог: `/tmp/slukhayka-546-full.log`.

Цей звіт не означає завершення інших UI-задач #547–551.
