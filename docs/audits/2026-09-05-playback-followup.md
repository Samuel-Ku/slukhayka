# Перевірка продовження завантажень і відновлення відтворення

Задача ще не пройшла фінальну перевірку на пристрої. Актуальний debug APK
зібрано, 126 вибраних тестів пройшли без помилок і пропусків. Повний набір
тестів не запускався.

SHA-256 APK:
`1210ad68aacc6ff7794ceec98ddc2079afb3bab54fbd4e2c55c0979154c177bd`.

## Що виправлено

- `StateFlow<AudiobookEntity>` губив зміни полів завантаження, бо вони не
  беруть участі в порівнянні entity. Вибрана книга тепер лишається `BookRow`
  до отримання Compose.
- Повторне завантаження каталогу скидало прогрес і `PAUSED` у наявній
  Library Entry. Каталог тепер атомарно зберігає ці поля.
- Оцінка розміру завантаження використовує той самий переданий диспетчер,
  що й черга. Тест скасування більше не залежить від випадкового завершення
  реального IO-потоку.
- Плеєр отримав спільні DNS і маршрутизацію через Media3 OkHttp datasource.
  Прямий WebView використовує локальний DNS-тунель. Явно вибраний Tor або
  проксі зберігає свій маршрут.
- Перевірка відновлення бере потрібний індекс лише відновлюваного Source.
  Публікація спільного профілю виконується після повернення до плеєра.
- Старі тести відтворення доповнені фізичними треками: передавання лише
  логічних розділів тепер правильно відхиляється захистом від підміни аудіо.

## Підтверджено

На попередньому тестовому пристрої збірка з виправленням StateFlow показала
«Продовжити завантаження: 43% готово» після скасування без виходу з картки.
Продовження повторно запускало чергу. Після повторного скасування лишилося
28 із 64 готових розділів. Це перевірка проміжного APK, до мережевих змін
і виправлення запису каталогу.

Останній прогін перевірив 18 класів або вибрані методи в них: скасування й
надійність завантажень; оновлення каталогу через Room; StateFlow картки;
ідентичність і вибір Source під час відновлення; DNS і маршрутизацію;
передавання байтів через CONNECT і HTTP; відмову тунелю при помилці DNS або
непрямому маршруті; обмеження автоматичного відновлення; підтвердження іншої
начитки; обробку помилок WebView; локальні та мережеві розділи, перехід між
ними, позицію, паузу під час буферизації та фактичну подію запуску плеєра.

## Перевірка на новому телефоні

На новому телефоні M2012K11AG (`192.168.2.169:39059`) встановлено debug APK
із наведеною вище контрольною сумою. Підтверджено:

- «Вбивча логіка», 4read: жодного локального треку; медіасесія PLAYING,
  позиція просувається. Користувач підтвердив чутний голос.
- Пауза; перемотування з 03:34 приблизно до 04:03; після force-stop картка
  показує «Продовжити з 04:03», повторний запуск грає зі збереженої позиції.
- «Проблема з миром»: після першого скасування зберігся один MP3,
  тимчасовий файл зник, картка одразу показала «Продовжити: 1%».
- Продовження додало наступні розділи. Після повторного скасування лишилося
  три готові MP3. Локальне відтворення підтверджене відкритим read-only
  файловим дескриптором першого MP3 у процесі плеєра.
- Другий і третій розділи перемикаються. Перемотування третього на 13:43
  при тривалості 13:49 привело до автоматичного запуску четвертого:
  PLAYING, позиція 00:06; четвертого MP3 немає.
- Додано контрольну закладку в першому розділі для перевірки відновлення.

## Наступна встановлена збірка

APK `45a013a7fabb6cfaceab72e810034f782b452dc1274876755885bdd39a3eca51`
установлено 2026-09-05 о 14:01 за місцевим часом. На ньому повторено
стрімінг 4read. Пошук нового sluhayua-джерела прибрано зі smart retry:
альтернативу шукає явна дія слухача.

Opt-in `LiveBrowserRecoveryTest` пройшов на пристрої:

- `saved_page_recovers_same_chapter_without_losing_local_data`: реальна
  сторінка 4read відновила зіпсоване лише в пам’яті аудіопосилання. Браузер
  закрився після `isPlaying=true`, розділ зберігся, позиція — 42 секунди.
  Ідентифікатори книг, закладки й хеші локальних файлів не змінилися.
- `failed_source_stops_old_audio_and_search_waits_for_action`: недоступне
  тестове джерело зупинило попереднє аудіо й завершило очікування. Видимі
  повтор і пошук альтернативи; пошук почався лише після явної дії.
  Подальша справжня книга заграла. Інша начитка викликала підтвердження й
  не підмінила поточну без згоди.
- `cancel_and_resume_preserve_completed_files_of_both_books`: скасування
  лишило стан PAUSED із додатним прогресом, готові файли обох книг
  зберегли свої хеші, тимчасові файли цільової книги прибрано. Продовження
  додало готовий розділ. Тест тривав 271 секунду.

Порівняння приватних даних виконується на пристрої без експорту бази.
«Вбивча логіка» після контрольного тесту вже повністю скачана; для наступної
перевірки повністю мережевої книги потрібна інша книга.

## Після рев’ю: нові зміни, що потребують фінальної збірки

- Відсутній трек зупиняє попередній звук і зберігає індекс та позицію
  запитаного розділу. Запізнілий результат відновлення не підміняє новий
  розділ; пауза під час відновлення зберігається.
- Тимчасові завантаження мають окремого власника (процес і покоління).
  Старий заблокований потік не просуває файл після скасування й не прибирає
  нову чергу. Зняття реєстрації перевіряє тотожність Job.
- HTTP-пули належать незмінним маршрутам; зміна маршруту скасовує також
  відкриті потоки. Media3, Coil і DoH більше не утримують прямий клієнт
  після вибору Tor або проксі.

Ці зміни ще не перевірені на встановленій фінальній збірці 1.3.9.
Локальні цільові тести скасування та надійності завантажень пройшли.
Прогін AudioPlayerManagerTest перервано через зависання Java під Rosetta
на safepoint; 14 завершених тестів без помилок не є результатом всього класу.
Web: 34 файли, 219 тестів пройшли; production build пройшов.

Живий Tor/проксі та повернення мережі ще не перевірено. Налаштування Wi-Fi,
DNS, проксі й автоповороту телефона під час цієї роботи не змінювалися.
Рекомендацію публікувати 1.3.9 до закриття цих перевірок не надано.

## Фінальна debug-збірка 1.3.9

Установлена 2026-09-05 о 14:54, versionCode 9.
SHA-256: `1c7e8582c999de5585784aaf77c3236b06a2877a505a7f78ad6b67936089b200`.
Два JVM-тести TransportClients пройшли: прогрітий direct keep-alive не
обходить новий Tor/проксі; відкритий response body скасовується при зміні.
Статичне повторне рев’ю мережевих, файлових і плеєрних виправлень завершене
без нового конкретного блокера.

На цій установленій збірці відновлення пройшло за 13,669 секунди, включно
зі збереженням розділу, позиції, бібліотеки, закладок і хешів файлів.
Тест невдалого джерела двічі дійшов до завершеного стану помилки, але не
побачив кнопок. Перевірка `dumpsys` і екрана встановила: телефон заснув і
вимагає PIN/відбиток. Результат UI не зараховано; очікується розблокування.
Другий JVM-прогін зі спрощеним JIT теж завис у Java safepoint під Rosetta;
запущено повтор без JIT. Відмови JVM не видані за успіх тестів.

Push кандидата відхилено автоматичним контролем дозволів. Перевірено
публічний `Samuel-Ku/slukhayka` і адміністративний доступ; після повторної
відмови запитано конкретне підтвердження комітів та гілки. CI поки не
запускався. Тег і публічний реліз не створені.

Повтор без JIT завершився успішно: усі 45 тестів AudioPlayerManagerTest
пройшли, без помилок і пропусків (65,421 секунди виконання тестів).
Тест продовження завантаження на пристрої вичерпав 120 секунд до нового
готового розділу. У коді знайдено послідовну перевірку HEAD усіх розділів
перед початком скачування. Оцінку розміру прибрано з обов’язкового шляху:
використовується вже готова оцінка картки або чесний невідомий загальний
розмір. Ця наступна зміна потребує тесту та нового встановленого APK.

Останній APK після прибирання HEAD із запуску: 1.3.9 / code 9,
установлено о 15:14, SHA-256
`0438d0bf35896be12bd1b4360ba107f07ae6aefddbca96b5c4da039dedcf540b`.
Пройшли 11 тестів OfflineDownloadsReliabilityTest і повторні 7
CancelDownloadTest, без помилок і пропусків; compileReleaseKotlin пройшов.
Телефонний таймаут повторився, отже HEAD не був повним поясненням збою.
До opt-in тесту додано діагностику завершення черги, стану та байтів.
Окремий opt-in тест приватних маршрутів підготовлено, але не запущено:
він вимагає `liveRouteTest=true` та окремого дозволу користувача.

Подовжений телефонний прогін завершився успішно за 140,643 секунди.
Під час нього тимчасовий файл виріс із 9 666 560 до 16 285 696 байтів:
черга справді отримувала аудіо. Ліміт попереднього тесту 120 секунд був
закоротким для цього розділу; у тесті доступний явний обмежений параметр
`liveDownloadWaitMs` (30–600 секунд), стандартний ліміт не змінено.
Новий розділ завершився; після скасування PAUSED і додатний прогрес
підтверджені, хеші раніше готових файлів двох книг збереглися, temp-файлів
цільової книги немає. Це перевірено на APK `0438d0bf…cf540b`.
Запущено повний передрелізний Android-прогін шістьма наборами через
`scripts/test-all.sh`, локально з `-Xint`; результат ще очікується.
