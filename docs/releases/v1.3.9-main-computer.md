# Підготовка 1.3.9 на основному комп’ютері

Статус: кандидат, публікація ще не дозволена результатами перевірки.
Push очікує окремого підтвердження після відмови автоматичного контролю
дозволів. CI та підписаний кандидат поки не отримані.

Коміт коду кандидата: `505c59d7`. Локальна гілка:
`codex/release-1.3.9-preparation`; цільова віддалена: `release/v1.3.9`.
Робочі зміни збережені окремими комітами: специфікації рекомендацій,
виправлення відтворення, підготовка випуску та запуск скачування без
очікування оцінки розміру. Локальні приватні артефакти
виключені через `.gitignore`, фізично збережені на цьому комп’ютері.

Остання встановлена на OnePlus debug-збірка: 1.3.9 / code 9, SHA-256
`006d19d36d6541c86a2461aa4b0e8b187090c5f8159ce16bbaebb6cfcaeb8fc1`.
Вона містить захист від небезпечного об’єднання бібліотеки під час запуску.
Усі 11 адресних тестів захисту пройшли. На попередньому Xiaomi старий
код зменшив бібліотеку зі 144 до 141 картки; їхнє відновлення не підтверджене.
Повтор recovery на актуальній збірці пройшов за 9,352 секунди:
збережені розділ, позиція, бібліотека, закладки та хеші локальних файлів.
Тест обирає доріжку фактично вибраного Source. Чутний звук користувач підтвердив.

Передрелізна перевірка: 1140 pure JVM і 3 SDK 35 пройшли; із 438 SDK 36
пройшли 437, локалізоване очікування виправлено, всі 4 тести класу пройшли.
RoomNativeDefault: 1 пройшов, 3 Firestore-тести пропущено без Emulator.
RoomRobolectricOnly: 293 із 295 пройшли; після виправлення локалі всі 7
навігаційних тестів пройшли. Також пройшли адресні перевірки плеєра,
скачування, мовного імпорту та recovery; 219 web-тестів і web build.
ComposeRoborazzi, повний CI, покриття й підписаний кандидат ще потрібні.
Це не дозвіл публікувати випуск.

## Після push і зеленого CI

На основному комп’ютері спочатку збережіть власні незакомічені зміни.
Далі в чистому checkout цього репозиторію:

```sh
git fetch origin --tags
git switch --track origin/release/v1.3.9
git status --short
git log -1 --oneline
```

Якщо локальна `release/v1.3.9` вже існує, перейдіть на неї й оновіть через
`git pull --ff-only`. Не зливайте сюди `main`.

У GitHub Actions відкрийте `CI` для точного коміту гілки. Мають пройти всі
шість тестових частин, покриття, рекомендаційна оцінка й signed-smoke build.
Завантажте артефакт `slukhayka-v1.3.9-signed-smoke`, перевірте SHA-256 та
підпис. Ключ має бути тим самим, що у встановленої release-версії; не
генеруйте новий keystore.

Перевірений відбиток сертифіката опублікованого APK 1.3.8 (SHA-256):
`08587854ce382fb53dca906edf3eadca497506b1f9166cd8b6483b8f47d2783a`.
Контрольна сума самого завантаженого 1.3.8 збіглася з вкладенням релізу;
`apksigner verify --print-certs` підтвердив підпис. Для кандидата виконайте
цю саму команду Android SDK Build Tools й порівняйте SHA-256 сертифіката. Секрети підпису вже налаштовані в GitHub.

Кандидат слід перевірити як оновлення наявної release-версії без видалення.
Debug-перевірка використовує окремий пакет і сама по собі не підтверджує
оновлення даних release-пакета. Результати сценаріїв:
[аудит відтворення](../audits/2026-09-05-playback-followup.md).

## Публікація після завершення перевірок

Ці команди ще не виконувалися. Push тега автоматично створить публічний
GitHub Release, тому виконуйте їх лише для перевіреного коміту:

```sh
git tag -a v1.3.9 -m 'Слухайка 1.3.9'
git push origin v1.3.9
```

Workflow `Release` збере підписаний APK, додасть контрольну суму й текст із
`docs/releases/v1.3.9.md`. Перевірте вкладення та встановлення оновлення.
Якщо перевірка кандидата не пройшла, не створюйте тег: виправлення мають
отримати новий коміт і власний зелений прогін CI.
